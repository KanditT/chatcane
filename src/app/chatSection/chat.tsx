import React from 'react';

const Chat: React.FC = () => {
    return (
        <div className=" bg-white   ">
            {/* Chat content without scroll bar */}
        </div>
    );
};

export default Chat;
